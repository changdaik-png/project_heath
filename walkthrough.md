# 작업 완료 보고서 (Walkthrough)

본 문서는 한국의료패널(KHP) 데이터 전처리, 모델링, 웹 대시보드 시각화 파이프라인 구축 과정에서 발생한 **"40대 필터링 미적용 및 80대/90대 환자 노출 버그"**, **"개인 식별자 기준 중복 데이터 제거"**, **"실무자 전용 UI 개선 및 데모 데이터 영역 제거"**, **"Git LFS를 활용한 대용량 모델 파일 원격 배포"**, **"Streamlit Cloud 서버 환경 버전 불일치로 인한 AttributeError 핫픽스"**, **"40대 코호트 전용 의료비 예측 모델 고도화 및 재학습"**, 그리고 **"사이드바 연소득 단위 불일치 핫픽스"** 패치 내역을 최종 요약 보고합니다.

---

## 1. 작업 개요
* **목적**: 기존 모델이 80~90대 고령층의 압도적인 의료비 지출 아웃라이어에 오염(Bias)되어 예측치가 과다 계산되던 구조적 문제를 해결하기 위해, 만 40세~49세(40대 코호트) 데이터만을 엄격하게 필터링하고 개인 식별자(`PIDWON`) 기준 중복 제거를 적용하여 모형을 완전히 재학습시키고 배포함.
* **수행 내용**:
  1. **전처리 모듈(`preprocessing.py`) 수정**:
     * 결측치 임의 보정 시 나이 범위를 기존 `[20, 85]`에서 `[40, 49]`로 좁혀 40대 정책 코호트와의 정합성을 일치시킴.
     * 나이 필터링을 기존 `A5 >= 40`에서 `(A5 >= 40) & (A5 <= 49)`로 제한하여 고령층의 수천만 원대 이상 의료비 아웃라이어가 유입되는 통로를 완전히 차단함.
     * 1대N 병합에 따른 인원수 부풀림을 배제하기 위해 개인식별자(`PIDWON`) 기준 `drop_duplicates`를 적용함.
  2. **모델 재학습(`train_model.py`) 실행**:
     * 40대 장년층 코호트로만 전처리된 깨끗한 데이터를 활용하여 랜덤 포레스트 모형을 완전히 재학습시키고 `model.pkl`에 덮어쓰기 저장 완료함.
     * 재학습된 40대 전용 모델은 고령층 의료비 지입에 따른 오염이 제거되어, MAE(평균 절대 오차)가 **2,901,071.32 원**으로 안정화되었으며 가장 결정적인 Feature가 `Income` (소득: 67.19%) 및 `Age` (나이: 23.46%) 순인 것을 확인하여 터미널에 정상 출력함.
  3. **대시보드 모듈(`app.py`) 검증**:
     * 공단 실무자가 2023년 데이터 등 새로운 통합 파일 업로드 시에도 내부 조인 로직에서 동일하게 '만 40세~49세 제한' 및 'PIDWON_STR 기준 중복 제거'가 선제 적용된 후 예측이 흐르도록 정비 완료함.
  4. **Git LFS 원격 갱신 Push**:
     * 수정된 코드 파일 및 새롭게 갱신된 `model.pkl` LFS 오브젝트를 GitHub 원격 저장소(`main` 브랜치)에 무사히 Push 가동하여 배포를 완료함.
  5. **Windows 터미널 cp949 인코딩 오류 핫픽스 (`app.py`)**:
     * 실무자용 업로드 기능 실행 시, 콘솔 출력(`print`)에 포함된 유니코드 이모지(`🏥`)로 인해 Windows 터미널(`cp949` 인코딩)에서 인코딩 에러가 발생하는 문제를 감지함.
     * 콘솔 출력문에서 이모지를 제거하고 일반 한글 텍스트 문구로 수정하여 파일 업로드 및 병합 프로세스가 중단 없이 원활하게 구동되도록 핫픽스를 반영함.
  6. **사이드바 소득 단위 불일치 오류 핫픽스 (`app.py`)**:
     * 좌측 사이드바의 소득 슬라이더가 월소득(백만원 단위) 기준인 반면, 학습된 모델 및 우측 패널 데이터는 연소득(만원 단위) 기준으로 관리되고 있어 단위 불일치로 인해 예측 의료비가 비현실적인 음수(-400만 원 등)로 튀는 버그를 발견함.
     * 사이드바 슬라이더를 '가구 연소득 선택 (만원 단위)'으로 명칭 및 범위를 전면 변경(1,000만 원 ~ 1억 5,000만 원, 기본값 5,000만 원, 스텝 500만 원)하고 해당 값을 그대로 모델 피처로 입력하여 정합성을 완벽히 조율함.
  7. **소득 변수 결측치 처리 및 모델 과적합 규제 조치 (`preprocessing.py`, `train_model.py`)**:
     * KHP 데이터상에서 가구 총소득(`TOT_INC`)이 `0` 이하로 잡힌 비정상 결측 데이터(약 17%)에 의해 소득 분포가 강하게 왜곡되었으며, 기존 트리 모델의 깊이 제한이 없어 데이터가 희소한 고소득 구간(5,000만 원 이상)에서 예측치가 불연속적으로 널뛰는 버그를 발견함.
     * `0` 이하 소득값을 `NaN` 처리하여 중간값(`median`)으로 안전하게 보완되도록 유도하고, 랜덤 포레스트 모델에 `max_depth=4` 및 `min_samples_leaf=50` 규제를 적용하여 널뛰기 없는 부드럽고 합리적인 예측 곡선이 형성되도록 전면 개선함.
  8. **공공 보건 정책 매칭(액션 플랜) 분기 로직 정비 (`app.py`)**:
     * 의료비 변동액이 급증하는 고위험 가구 중, 가구 연소득이 4,000만 원 이하인 경우를 복지 지원 혜택을 받는 `Track 2` (선제적 의료 바우처 지급 및 복지 사각지대 해소)로 확실히 분류하고, 4,000만 원을 초과하는 중고소득 가구만 `Track 1` (선제적 예방 및 조기 검진)로 매핑되도록 정밀 조율함.

---

## 2. 모델 재학습 성능 지표 요약

### ① 40대 코호트 데이터셋 요약
* **전체 샘플수**: 12,909건
* **결측 제거 및 40대 필터링 후 가용 샘플**: 12,516건 (Train: 8,761건 / Test: 3,755건)
  * *참고: 기존 0원 이하 소득 결측 가구(2,506건)는 이제 모델 임퓨터(`SimpleImputer(strategy='median')`)를 거쳐 중간값으로 보완되어 학습에 안정적으로 활용됨.*

### ② 학습 결과 (40대 전용 규제형 랜덤 포레스트 회귀)
* **평균 절대 오차 (MAE)**: **2,565,719.39 원** (이전 버스트 방지 및 규제화로 모델 성능 향상 및 안정화)
* **평균 제곱근 오차 (RMSE)**: 4,658,789.42 원
* **변수 중요도 (Feature Importance)**:
  1. **Income (가구소득)**: **85.51%**
  2. **Age (만 나이)**: **10.71%**
  3. **Gender (성별)**: **3.78%**
  * 규제 파라미터 적용 후, 변수 중요도 1위인 `Income`이 실제 5년 뒤 의료비 변동의 가장 결정적인 설명력을 지니고 있음을 강력히 실증하고 있습니다.

---

## 3. 리소스 파일 링크

* [preprocessing.py](file:///c:/Users/kcd11/OneDrive/바탕 화면/sujungpro/preprocessing.py): 만 40세~49세 필터 및 `PIDWON` 중복제거가 내장된 전처리 파이프라인 모듈.
* [train_model.py](file:///c:/Users/kcd11/OneDrive/바탕 화면/sujungpro/train_model.py): 재학습 지표 출력 및 새로운 `model.pkl` 저장을 가동하는 훈련 스크립트.
* [app.py](file:///c:/Users/kcd11/OneDrive/바탕 화면/sujungpro/app.py): 실무자용 업로드 로직에 연령 필터링 및 중복 제거가 엄격하게 탑재된 대시보드.
* [model.pkl](file:///c:/Users/kcd11/OneDrive/바탕 화면/sujungpro/model.pkl): 재학습 완료되어 깃허브 LFS에 갱신 적용된 40대 코호트 전용 예측 모델.
